version: '3.9'

services:
  zephyr-backend:
    build:
      context: .
      dockerfile: ./Zephyr.Backend/Dockerfile
    ports:
      - "80:8080"
    env_file:
      - ./.env
    restart: unless-stopped
